import React from 'react'

export const Balance = () => {
   
  

    // const amounts = transactions.map(transaction => transaction.Amount);
  
    // const total = amounts.reduce((acc, item) => (acc += item), 0).toFixed(2);
    
    return (
        <div>
            <h3>Your Balance <br/> 19383</h3>
        </div>
    )
}